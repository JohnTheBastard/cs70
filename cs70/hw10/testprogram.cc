#include <cassert>
#include <iostream>
#include <string>
#include "hashtable.hh"

using namespace std;

int main(int, char *[])
{
  HashTable foo;
  /*
  foo.insert("a");
  foo.insert("aa");
  foo.insert("ab");
  foo.insert("b");
  foo.insert("bb");
  foo.insert("bc");
  foo.insert("c");
  foo.insert("cc");
  foo.insert("cccc");
  foo.insert("a");
  
  assert(foo.lookUp("a") != NULL);

  foo.printStats();
  */
  return 0;
}
